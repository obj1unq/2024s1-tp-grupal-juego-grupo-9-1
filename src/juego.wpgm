import wollok.game.*
import hero.*
import jefe.*

program juego {
	game.width(15)
	game.height(12)
	game.start()
	
	/*
	 * estado del game utilizado para testear el boss
	game.title("game")
	game.width(15)
	game.height(12)
	game.boardGround("fondo.png")
	game.addVisualCharacter(hero)
	game.addVisual(jefe)
	jefe.iniciar()
	keyboard.o().onPressDo({jefe.recibirDanio(10)})
	game.start() */
}